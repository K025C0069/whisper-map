import MapScreen from "@/components/MapScreen";

// This page is no longer used by the router, but we keep it as a
// simple passthrough so it compiles without errors. The logic was
// migrated to MapScreen and the mission API was expanded.
export default MapScreen;
